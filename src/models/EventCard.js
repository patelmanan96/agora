function EventCard(img, title, tag, summary, desc, othertags, date) {
    this.image = img;
    this.title = title;
    this.tag = tag;
    this.summary = summary;
    this.description = desc;
    this.othertags = othertags;
    this.date = date;
}
export default EventCard;